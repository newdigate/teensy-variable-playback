get_filename_component(CURRENT_FOLDER_NAME ${CMAKE_SOURCE_DIR} NAME)
if(CURRENT_FOLDER_NAME STREQUAL "examples")
    set(TEENSY_VARIABLE_PLAYBACK_ROOT ${CMAKE_SOURCE_DIR}/../)
else()
    set(TEENSY_VARIABLE_PLAYBACK_ROOT ${CMAKE_SOURCE_DIR}/../../)
endif()

import_arduino_library_absolute(${TEENSY_VARIABLE_PLAYBACK_ROOT}/src)
